# Fuentes locales

Para uso 100% offline con tipografías Renta Ya, coloca aquí:

- `Fraunces-Variable.woff2`
- `SpaceGrotesk-Variable.woff2`

Mientras tanto, `_fonts.scss` usa `local()` + respaldos del sistema (Georgia / Segoe UI).
