# Red escolar / antivirus suele interceptar HTTPS y rompe el certificado de npm.
# Esto permite instalar dependencias locales. No afecta el runtime de la app.
strict-ssl=false
fund=false
legacy-peer-deps=true
